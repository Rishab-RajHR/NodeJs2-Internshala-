const myArr2 = ['one','two','three','two','three','two','three','two','three']

// get the lastIndex
val = myArr2.lastIndexOf('two')
console.log(myArr2);

// Sorting of the data (Alphabetically)
myArr2.sort();
// Sorting of the data (Not alphabetically)
myArr2.reverse();
console.log(myArr2);

console.log(val);