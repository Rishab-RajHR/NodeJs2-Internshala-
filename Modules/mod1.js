const test1 = require('./test1');
console.log(module.filename);

test1.test1();
console.log(test1);
console.log(test2);
const myName = `${test.person.first} ${test.person.last}`;
const myName2 = `${test.person.first} ${test.person.last}`;
console.log(myName);
console.log(myName2);
