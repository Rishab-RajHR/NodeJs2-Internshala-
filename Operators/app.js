let val = 100;
val = 1000;
val = val + 1;  // val += 1 , val++ all are same
val = val - 1;  // val -= 1 , val-- all are same
val = val * 2;  // val *= 2
val = val / 3;  // val /= 3
console.log(val*10)

console.log(val);